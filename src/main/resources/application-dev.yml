spring:
  datasource:
    url: jdbc:postgresql://db:5432/cribhub
    username: postgres
    password: password
  profiles:
    active: dev
  jpa:
    hibernate:
      ddl-auto: create
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect